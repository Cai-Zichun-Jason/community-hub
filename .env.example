# ===================================
# Community Hub 环境变量配置示例
# ===================================
#
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改下面的密码和配置
# 3. 不要将 .env 文件提交到 git
#
# ===================================

# ===================================
# 全局配置
# ===================================
# 全局强密码（所有服务共用，方便记忆且安全）
# 建议：包含大小写字母、数字、特殊字符，长度 16 位以上
GLOBAL_PASSWORD=your_strong_password_here

# ===================================
# MySQL 数据库配置
# ===================================
# MySQL 根用户（系统限制必须为 root）
MYSQL_ROOT_USER=root
MYSQL_ROOT_PASSWORD=${GLOBAL_PASSWORD}

# BBS-GO 数据库
MYSQL_BBSGO_DATABASE=bbsgo
MYSQL_BBSGO_USER=bbsgo
MYSQL_BBSGO_PASSWORD=${GLOBAL_PASSWORD}

# BookStack 数据库
MYSQL_BOOKSTACK_DATABASE=bookstack
MYSQL_BOOKSTACK_USER=bookstack
MYSQL_BOOKSTACK_PASSWORD=${GLOBAL_PASSWORD}

# Homarr 数据库
MYSQL_HOMARR_DATABASE=homarr
MYSQL_HOMARR_USER=homarr
MYSQL_HOMARR_PASSWORD=${GLOBAL_PASSWORD}

# ===================================
# MinIO 对象存储配置
# ===================================
# MinIO 根用户
MINIO_ROOT_USER=root
MINIO_ROOT_PASSWORD=${GLOBAL_PASSWORD}

# BBS-GO 存储桶和访问凭证
MINIO_BBSGO_BUCKET=bbsgo
MINIO_BBSGO_ACCESS_KEY=bbsgo
MINIO_BBSGO_SECRET_KEY=${GLOBAL_PASSWORD}

# BookStack 存储桶和访问凭证
MINIO_BOOKSTACK_BUCKET=bookstack
MINIO_BOOKSTACK_ACCESS_KEY=bookstack
MINIO_BOOKSTACK_SECRET_KEY=${GLOBAL_PASSWORD}

# Homarr 存储桶和访问凭证
MINIO_HOMARR_BUCKET=homarr
MINIO_HOMARR_ACCESS_KEY=homarr
MINIO_HOMARR_SECRET_KEY=${GLOBAL_PASSWORD}

# ===================================
# 端口配置
# ===================================
# 存储层
MYSQL_PORT=9021
MINIO_API_PORT=9022
MINIO_CONSOLE_PORT=9001

# 服务层
BBSGO_API_PORT=8082
HOMARR_PORT=8081
BOOKSTACK_PORT=8083
CLASH_PORT=9002
MIHOMO_PORT=9023

# ===================================
# 管理员账号配置
# ===================================
# 所有服务的超级管理员统一配置
ADMIN_USERNAME=SLAI
ADMIN_PASSWORD=${GLOBAL_PASSWORD}
ADMIN_EMAIL=admin@slai.local

# ===================================
# 密码安全建议
# ===================================
# 1. 使用强密码：至少 16 位，包含大小写字母、数字、特殊字符
# 2. 不要在多个系统间重复使用密码
# 3. 定期更换密码（建议每 90 天）
# 4. 生产环境必须使用随机生成的强密码
# 5. 可以使用密码管理器生成和保存密码
# ===================================
